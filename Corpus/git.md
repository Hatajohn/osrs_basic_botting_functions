# Git

- **Branch for this work:** `feature/linux-exodia-wslg`
- **Remote:** SSH `git@github.com:Hatajohn/osrs_basic_botting_functions.git` (HTTPS non-interactive push often fails in agents).
- **Author:** repo-local `user.name` / `user.email` should be set before committing (Step 0).
